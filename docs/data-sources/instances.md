---
page_title: "Data source: powersync_instances"
subcategory: ""
description: |-
  Lists all PowerSync instances in a project. Returns a lightweight summary per instance — for region, URL, status, or sync config use the powersync_instance data source on a specific id.
---

# Data source: powersync_instances

Lists all PowerSync instances in a project. Returns a lightweight summary per instance — for region, URL, status, or sync config use the `powersync_instance` data source on a specific id.

## Example Usage

```terraform
# List every instance under a project. Useful for inventory/auditing or to look
# up an instance ID by name without hard-coding it.
data "powersync_instances" "all" {
  org_id     = "64b3f8e1a2c4d5e6f7080912"
  project_id = "699ef9c371c56d0007320543"
}

output "instance_count" {
  value = length(data.powersync_instances.all.instances)
}

output "instance_summary" {
  value = [for i in data.powersync_instances.all.instances : {
    id         = i.id
    name       = i.name
    deployable = i.deployable
    has_config = i.has_config
  }]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `org_id` (String) Organization ID that owns the project.
- `project_id` (String) Project ID whose instances to list.

### Read-Only

- `instances` (Attributes List) Instances in the project. (see [below for nested schema](#nestedatt--instances))

<a id="nestedatt--instances"></a>
### Nested Schema for `instances`

Read-Only:

- `deployable` (Boolean) Whether the instance is currently in a deployed state. `false` for new, deactivated, or destroyed instances.
- `has_config` (Boolean) Whether the instance has been configured (region, replication, client auth). `false` for newly created instances that have never been deployed.
- `id` (String) Instance ID.
- `name` (String) Instance name.
